# Cribl Health Check - Development Dependencies
# Install with: pip install -r requirements-dev.txt

# Include runtime dependencies
-r requirements.txt

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# HTTP mocking for tests
respx>=0.20.0

# Code formatting
black>=23.12.0

# Linting (pinned: unpinned linters drift and break CI on unrelated PRs)
ruff==0.16.8

# Type checking (pinned, see above)
mypy==2.3.1

# Build tools
build>=1.0.0
twine>=4.0.0
